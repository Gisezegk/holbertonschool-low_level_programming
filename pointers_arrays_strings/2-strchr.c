#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 *_strchr - locates a character in a string
 *@s: value
 *@c: value
 *Return: Always 0;
 */
char *_strchr(char *s, char c)
{
	while (*s != '\0')
	{
		if
	(*s == c) return (char *) s;
		s++;
	}

	return (NULL);
	{
	if (s == NULL)

return (" ");
	}
}
