#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 **_strcpy - copies the string pointed to by src
 *
 */

char *_strcpy(char *dest, char *src)
{
{
	char *ptr = strcpy(dest, src);
	printf("%s", ptr);

}

return (0);
}

