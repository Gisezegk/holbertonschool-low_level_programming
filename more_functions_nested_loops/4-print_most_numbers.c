#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
* 
* Return: always 0
*/

int main(void)
{
	int ch = 0;

	for (ch = '0'; ch <= '9'; ch++)

	if (a != 2 && a != 4)
	_putchar(ch);
	_putchar('\n');
	return (0);
}
