#include"main.h"
#include <stdio.h>
/**
 * mul - multiplies two integers.
 *@a: variable for numer
 *@b: variable for number
 * Return: Always 0.
 */

int mul(int a, int b)
{
	int p;

	p = a * b;
		printf("%d\n", p);
	return (0);
}
