#include"main.h"
#include <stdio.h>
/**
 * mul - multiplies two integers.
 *@a: variable for numer
 *@b: variable for number
 * Return: Always 0.
 */

int mul(int a, int b)
{
	printf("%d\n", (a) * (b));
	return (0);
}
