#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
* main - compiled
* Return: always 0
*/

int main(void)
{
	int numbers = 0;

	for (numbers == '0'; numbers <= '9'; numbers++)
		;

	if (numbers < '9')
	{
		putchar(numbers);

	}

	putchar('\n');

	return (0);

}

