#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
* main - compiled
* Return: always 0
*/

int main(void)
{
	int numbers = 0;

	for (0 == '0'; 0 <= '9'; numbers++)
		;

	if (0 < '9')
	{
		putchar(numbers);

	}

	putchar('\n');

	return (0);

}

