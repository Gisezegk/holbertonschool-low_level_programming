#include <stdlib.h>
#include <stdio.h>

int
main(int argc, char* argv[])
{
	int i;
	int sum;

	if (argc > 0)
	
	{
for (i = 1; i < argc; i++)
{
	sum += atoi(argv[i]);
		}

}
	i = atoi(argv[i]);

	printf("%d\n", sum);
return (0);
}
