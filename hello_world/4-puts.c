#!/bin/bash
#include<stdio.h>

/**
 * main - Use the function puts
 * Return: always 0
 */
 
int main(void)
{
char gise[] = "\"Programming is like building a multilingual puzzle";

puts(alow);

return (0);
}
