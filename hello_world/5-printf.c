#include<stdio.h>

/**
 * main - Use the function printf
 * Return: always 0
 */

int main(void)
{
char impres[] = "with proper grammar, but the outcome is a piece of art/,";

puts(impres);

return (0);
}



